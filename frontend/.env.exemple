# URL completa do Node (opcional). Se vazio, em produção usa automaticamente:
# mesmo host do navegador + REACT_APP_BACKEND_PORT (padrão 8080).
# REACT_APP_BACKEND_URL=http://127.0.0.1:8080
REACT_APP_BACKEND_PORT=8080
REACT_APP_HOURS_CLOSE_TICKETS_AUTO=24
